# GNN Autoencoder
A basic autoencoder using GCNConv, which creates latent representations of enzymes from TUDataset

## Setup
Clone the repo, then install the requirements as shown in requirements.txt, CD to src, then run train.py